Drop the real ORÈVA asset files in here, using these exact names:

  logo.png   — the ORÈVA logo (used in the sidebar and the login screen)
  bg.png     — optional page background

Neither file was included in the upload used to build this admin panel, so
a placeholder text mark ("O") is shown until logo.png is added, and a solid
paper-colour background is used until bg.png is added. Both fall back
automatically — nothing needs to change in the code once you add the files
here, and no broken-image icon will ever show.
